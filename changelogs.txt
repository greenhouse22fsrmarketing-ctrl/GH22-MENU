Changelogs

v0.0.1
- Fixed return button in mobile version, now returns to Categories Selection instead of exiting the whole page. Pressing return in Categories Selection will now cause the page to exit.
- Added a "Press To Call Us" button in Socials sections that when pressed, directs to phone dial with number embedded.
- Made the phone number in the footer be clickable, same behavior as above.
- Added a site version to track changes. Previous versions will be backed up.

v0.0.2
- Fixed scrollable menu starting where the previous menu scroll end

